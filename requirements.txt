# ============================================================
# HiringBase — Python Dependencies
# Last updated: 2026-05-10 | Tested with Python 3.12+
# Versi aktual: venv/bin/pip freeze
# ============================================================

# ── FastAPI + Async Server ──────────────────────────────────
fastapi>=0.136.0
uvicorn[standard]>=0.46.0

# ── Database ────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.49
asyncpg>=0.31.0
alembic>=1.18.0

# ── Validation & Serialization ──────────────────────────────
pydantic>=2.13.0
pydantic-settings>=2.14.0
email-validator>=2.3.0

# ── Authentication & Security ───────────────────────────────
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.0          # Pinned: versi lebih baru inkompatibel dengan passlib
python-multipart>=0.0.27

# ── AI / NLP / ML ───────────────────────────────────────────
sentence-transformers>=5.4.0
scikit-learn>=1.8.0
numpy>=2.4.0

# ── OCR ─────────────────────────────────────────────────────
# Using Mistral AI OCR via httpx (api.mistral.ai/v1/ocr)
pillow>=12.2.0

# ── HTTP Client ─────────────────────────────────────────────
# httpx digunakan untuk LLM API calls (Groq) dan test client
httpx>=0.28.0
aiohttp>=3.13.0
anyio>=4.13.0            # Async I/O abstraction layer

# ── Cloud Storage (Cloudflare R2 — S3-compatible) ───────────
boto3>=1.43.0
botocore>=1.43.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.2.0
tenacity>=9.1.0
structlog>=25.5.0
orjson>=3.11.0

# ── Testing ─────────────────────────────────────────────────
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
factory-boy>=3.3.0
faker>=40.15.0

# ── Development / Lint (opsional, tidak wajib di production) ─
black>=26.3.0
ruff>=0.15.0
mypy>=1.20.0

# ── Background Tasks ────────────────────────────────────────
taskiq>=0.12.0
taskiq-redis>=1.2.0
taskiq-fastapi>=0.5.0
pycron>=3.2.0
