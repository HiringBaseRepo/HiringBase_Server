# ============================================================
# HiringBase — Python Dependencies
# Last updated: 2026-05-12 | Tested with Python 3.12+
# ============================================================

# ── FastAPI + Async Server ──────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Database ────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0

# ── Validation & Serialization ──────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# ── Authentication & Security ───────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.0          # Pinned: required by passlib as per AGENTS.md
python-multipart>=0.0.9

# ── AI / NLP / ML ───────────────────────────────────────────
# (Local AI libraries removed - moved to Hugging Face Inference API)

# ── OCR & Document Processing ───────────────────────────────
# Using Mistral AI OCR via httpx
pillow>=10.3.0

# ── HTTP Client & API Calls ─────────────────────────────────
# Used for Groq LLM, Mistral OCR, and Test Client
httpx>=0.27.0
aiohttp>=3.9.0
anyio>=4.3.0

# ── Cloud Storage (Cloudflare R2 — S3-compatible) ───────────
boto3>=1.34.0
botocore>=1.34.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0.1
tenacity>=8.3.0
structlog>=24.1.0
orjson>=3.10.0

# ── Background Tasks ────────────────────────────────────────
taskiq>=0.11.0
taskiq-redis>=0.5.0
taskiq-fastapi>=0.3.0
pycron>=3.0.0
redis>=5.0.0

# ── Testing ─────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
factory-boy>=3.3.0
faker>=25.0.0

# ── Development / Lint ──────────────────────────────────────
black>=24.4.0
ruff>=0.4.0
mypy>=1.10.0
